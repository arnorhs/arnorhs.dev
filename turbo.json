{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "export": {
      "dependsOn": ["^build", "build", "^export"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typegen": {
      "dependsOn": ["^build"],
      "inputs": ["wrangler.jsonc"],
      "outputs": ["worker-configuration.d.ts"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck", "^build", "typegen"]
    },
    "dev": {
      "dependsOn": ["^build", "typegen"],
      "persistent": true,
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
